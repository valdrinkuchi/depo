<%= form_with(model: productcategory, local: true) do |form| %>
  <% if productcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(productcategory.errors.count, "error") %> prohibited this productcategory from being saved:</h2>

      <ul>
      <% productcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if productcategory.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', productcategory, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if productcategory.persisted? %>
      <%= link_to "Cancel", productcategory, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", productcategories_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
